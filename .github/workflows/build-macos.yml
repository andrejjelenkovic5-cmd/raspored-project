name: Test macOS

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: macos-15

    steps:
      - name: Test macOS
        run: |
          echo "MAC RUNNER RADI"
          uname -m
          sw_vers
